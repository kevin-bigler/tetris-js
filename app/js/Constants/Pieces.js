/**
 * Define the active pieces (names)
 * Names must match resources/pieces/*.csv file names
 * (without the extension)
 * @type {[string...]}
 */
const Pieces = [
    'I',
    'J',
    'L',
    'S',
    'Square',
    'T',
    'Z'
];

export default Pieces;